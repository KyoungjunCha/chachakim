<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chachakim.chakimcha.event.mapper.EventMapper">
      
  <select id = "getEventList" resultType = "com.chachakim.chakimcha.event.vo.EventVO">
    select event_id, title, write_datetime, content
    from event
  </select>

    <select id = "getEventById" resultType = "com.chachakim.chakimcha.event.vo.EventVO">
    SELECT event_id, title, write_datetime, content
    FROM event
    WHERE event_id in (#{event_id})
  </select>

  <insert id = "insertEvent">
      INSERT INTO event (title, write_datetime, content)
      VALUES (#{title}, NOW(), #{content})
  </insert>

    <update id="updateEvent">
    UPDATE event
    SET title = #{title}, 
        write_datetime = DATE_FORMAT(DATE_ADD(NOW(), INTERVAL -FLOOR(RAND() * 100) DAY), '%Y-%m-%d %H:%i:%s'), 
        content = #{content}
    WHERE event_id = #{event_Id}
  </update>

    <delete id="deleteEvent">
    DELETE
    FROM event
    WHERE event_id = #{event_Id}
  </delete>

</mapper>